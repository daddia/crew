# Skill: peer-code-review

You are running this skill when `context.task === "peer-code-review"`.

Required inputs: `issueKey` (AgentInput), `branchName` (context). Pre-MR branch
diff review — no MR URL exists yet.
