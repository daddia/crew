# {{PACKAGE_NAME}}

Server-shaped crew scaffolded with `crew init {{CREW_NAME}} --shape server`.

## Next steps

1. Define workflow steps in `src/workflow.ts`.
2. Implement persona logic in `src/agents/engineer/`.
3. Add webhook handlers under `src/handlers/` when ready.
4. Wire smoke eval in `evals/smoke.eval.ts` once `@daddia/crew/evals` ships (RH02-05).
