/**
 * Smoke eval stub for {{CREW_NAME}}.
 *
 * `@daddia/crew/evals` is not shipped yet (RH02-05). When it lands, replace
 * this file with:
 *
 * ```typescript
 * import { defineEval } from '@daddia/crew/evals';
 *
 * export default defineEval({
 *   name: 'smoke',
 *   async run(t) {
 *     t.succeeded();
 *   },
 * });
 * ```
 */
export const smokeEvalPendingSubpath = '@daddia/crew/evals' as const;
