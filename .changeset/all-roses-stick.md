---
"@daddia/crew": minor
---

Add @daddia/crew/config subpath export with loadEnv, loadYaml, Secret/redact primitives, detectWorkspace, ConfigNotFoundError, and SchemaValidationError. Adds zod ^4 and yaml as runtime dependencies.
